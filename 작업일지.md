# 만세력 풀이 - 작업일지

> 작업일: 2026-03-08

---

## 1. 프로젝트 초기 설정

- Next.js 16.1.6 + React 19 + TypeScript + Tailwind CSS 4 기반 프로젝트 생성 (`create-next-app`)
- 만세력 계산 라이브러리 `@fullstackfamily/manseryeok` 의존성 추가
- 보안 헤더 설정 (`next.config.ts`): X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy

---

## 2. 사주 계산 엔진 구현

### 핵심 로직 (`src/lib/saju/`)

| 파일 | 역할 |
|------|------|
| `index.ts` | 전체 사주 분석 메인 함수 (`analyzeSaju`) — 음력→양력 변환 포함 |
| `fourPillars.ts` | 사주 사기둥(년·월·일·시주) 계산 |
| `elements.ts` | 오행 밸런스 계산, 용신 추정 |
| `tenGods.ts` | 십신(십성) 계산 |
| `interactions.ts` | 합충형파해 분석 (천간합/충, 지지삼합/방합/육합/충/형/파/해) |
| `specialStars.ts` | 신살 분석 |
| `daeun.ts` | 대운 계산, 세운/월운 계산, 순역행 판단 |
| `coreSummary.ts` | 격국 + 십신 종합 핵심 요약 |

### 상수 테이블 (`src/lib/constants/`)

| 파일 | 역할 |
|------|------|
| `stems.ts` | 천간 데이터 (한글, 한자, 오행, 음양) |
| `branches.ts` | 지지 데이터 (한글, 한자, 띠, 오행, 음양, 장간) |
| `lookupTables.ts` | 십성·12운성·공망 조회 테이블 |
| `solarTerms.ts` | 절기 데이터 |

### 타입 정의 (`src/types/saju.ts`)

- `Pillar`, `FourPillars` — 사주 기둥 구조
- `DaeunCycle`, `AnnualLuck`, `MonthlyLuck` — 대운/세운/월운
- `Interaction` — 합충형파해
- `SpecialStar` — 신살
- `ElementBalance` — 오행 밸런스
- `GongMang` — 공망
- `SajuResult` — 전체 분석 결과 통합 인터페이스

---

## 3. 해석 텍스트 데이터 (`src/data/interpretations/`)

| 파일 | 내용 |
|------|------|
| `dayMasters.ts` | 10개 일간별 성격·장단점 해석 |
| `tenGods.ts` | 십신별 의미·해석 텍스트 |
| `specialStars.ts` | 신살별 의미·길흉 해석 |
| `interactions.ts` | 합충형파해 해석 텍스트 |
| `growthStages.ts` | 12운성 해석 텍스트 |

---

## 4. UI 컴포넌트 (`src/components/saju/`)

| 컴포넌트 | 역할 |
|----------|------|
| `FourPillarsCard.tsx` | 사주 사기둥 시각화 카드 |
| `PillarColumn.tsx` | 기둥 컬럼 공통 컴포넌트 |
| `CoreSummaryCard.tsx` | 격국 + 십신 종합 핵심 요약 카드 |
| `DayMasterCard.tsx` | 일간(나) 분석 카드 |
| `TenGodsSection.tsx` | 십신 분석 섹션 |
| `ElementBalance.tsx` | 오행 밸런스 차트 |
| `SpecialStarsSection.tsx` | 신살 섹션 |
| `DaeunTimeline.tsx` | 대운 타임라인 |
| `AnnualLuckList.tsx` | 세운(연운) 리스트 |
| `MonthlyLuckList.tsx` | 월운 리스트 |
| `GongMangSection.tsx` | 공망 섹션 |
| `InteractionsSection.tsx` | 합충형파해 섹션 |
| `luckInterpretations.ts` | 운세 해석 유틸리티 |

---

## 5. 페이지 구현

### 입력 페이지 (`src/app/page.tsx`)

- 성별 선택 (남/여)
- 양력/음력 전환 (음력 선택 시 평달/윤달 옵션 표시)
- 생년월일 입력 (YYYYMMDD 숫자만)
- 태어난 시간 입력 (HHMM, 선택사항)
- 다크 테마 UI (배경 `#0D0D14`, 보라-핑크-블루 그라디언트)

### 결과 페이지 (`src/app/result/page.tsx`)

- URL 쿼리 파라미터로 입력값 수신
- `analyzeSaju()` 호출하여 전체 분석
- 섹션 순서: 사주팔자 → 핵심요약 → 일간분석 → 십신 → 오행밸런스 → 신살 → 대운 → 세운 → 월운
- Suspense로 로딩 상태 처리
- sticky 헤더, "다시 입력" 네비게이션

---

## 6. SEO 및 메타데이터

- `layout.tsx`: Open Graph, Twitter Card 메타태그 설정
- `opengraph-image.tsx`: Edge Runtime 기반 동적 OG 이미지 생성 (☯ 배경, 오행 아이콘, 그라디언트 타이틀)
- `metadataBase`: `https://saju-manse.vercel.app`

---

## 7. 디자인 시스템 (`Saju.pen`)

- Pencil 기반 디자인 시스템 파일 작성
- 프로젝트명: "SAJU HUNTERS"
- 포함 컴포넌트: Buttons, Input Fields, Badges (오행별), Cards, Navigation, Section Headers, List Items, Progress Bars, Typography, Color Palette
- 다크 테마, Space Grotesk + Manrope 폰트, 보라-핑크 그라디언트 액센트

---

## 8. Vercel 배포 설정

- `.vercel` 디렉토리 존재 — Vercel 프로젝트 연결 완료
- 배포 URL: `https://saju-manse.vercel.app`

---

## 현재 상태

- 초기 커밋 1개 (`69b0e67`, 2026-03-08) 이후 모든 작업이 **미커밋** 상태
- 수정 파일 6개 + 신규 파일 다수 (컴포넌트, 라이브러리, 데이터, 타입 전체)
- Vercel 배포 설정 완료

---

## 다음 작업 (이어서 할 것)

- 미커밋 변경사항 정리 및 커밋
- `GongMangSection`, `InteractionsSection` 컴포넌트가 결과 페이지에서 미사용 — 연결 여부 확인
- 음력 입력 시 정상 변환 테스트
- 모바일 반응형 UI 점검
- Vercel 프로덕션 배포 및 동작 확인
